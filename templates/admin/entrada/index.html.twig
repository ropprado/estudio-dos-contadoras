{% extends 'admin/base.html.twig' %}

{% block title %}Entradas{% endblock %}

{% block body %}
    <div class="container-fluid">
      <!-- /.row -->
      <div class="row">
        <div class="col-12">
          <div class="card">
            <div class="card-header">
              <h3 class="card-title"> Entradas </h3>

              <div class="card-tools">
                <div class="input-group input-group-sm" style="width: 150px;">
                  <a href="{{ path('admin_entrada_new') }}"><button type="button" class="btn btn-outline-success">+</button></a>


                </div>
              </div>
            </div>
            <!-- /.card-header -->
            <div class="card-body table-responsive p-0">
              <table class="table table-hover text-nowrap">

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Título</th>
                <th>Fecha</th>
                <th>Acciones</th>
            </tr>
        </thead>
        <tbody>
        {% for entrada in entradas %}
            <tr>
                <td>{{ entrada.id }}</td>
                <td>{{ entrada.titulo }}</td>
                <td>{{ entrada.fecha ? entrada.fecha|date('Y-m-d H:i:s') : '' }}</td>
                <td>
                    <a href="{{ path('admin_entrada_show', {'id': entrada.id}) }}"><button type="button" class="btn btn-outline-info">Ver</button></a>
                    <a href="{{ path('admin_entrada_edit', {'id': entrada.id}) }}"><button type="button" class="btn btn-outline-warning">Editar</button></a>
                    {{ include('admin/entrada/_delete_form.html.twig') }}
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no hay resultados</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    </div>
            <!-- /.card-body -->
          </div>
          <!-- /.card -->
        </div>
      </div>
      <!-- /.row -->
    </div><!-- /.container-fluid -->

{% endblock %}
