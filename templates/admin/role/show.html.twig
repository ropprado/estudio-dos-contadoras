{% extends 'admin/base.html.twig' %}

{% block title %}Rol{% endblock %}

{% block body %}
    <h1>Rol</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ role.id }}</td>
            </tr>
            <tr>
                <th>Código</th>
                <td>{{ role.code }}</td>
            </tr>
            <tr>
                <th>Descripción</th>
                <td>{{ role.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_role_index') }}">volver al listado</a>

    <a href="{{ path('admin_role_edit', {'id': role.id}) }}">Editar</a>

    {{ include('admin/role/_delete_form.html.twig') }}
{% endblock %}
